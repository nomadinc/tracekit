import { ShellPlaceholder } from "@/components/layout/shell-placeholder";

export default function BrandingPage() {
  return <ShellPlaceholder title="Branding" purpose="The white-label foundation supports bounded identity and accessible accent configuration without redesigning TraceKit chrome." permission="branding.view" variants={["agency"]} managementPermission="branding.manage" />;
}
