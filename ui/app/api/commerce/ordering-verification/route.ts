import { randomUUID } from "node:crypto";
import { NextResponse } from "next/server";
import { resolveApplicationSession } from "@/lib/identity/application-session";
import { AuthorizationDeniedError, requirePermission } from "@/lib/identity/authorization-gateway";

const confirmation="verify-commas-provider-ordering";
const response=(requestId:string,body:Record<string,unknown>,status:number)=>NextResponse.json({...body,requestId},{status,headers:{"x-tracekit-request-id":requestId}});
export async function POST(request:Request){const requestId=randomUUID();try{const origin=request.headers.get("origin");if(origin&&origin!==new URL(request.url).origin)return response(requestId,{ok:false,code:"request_verification_failed"},403);const resolution=await resolveApplicationSession();if(resolution.kind!=="authenticated"||!resolution.session.activeOrganization)return response(requestId,{ok:false,code:"resource_unavailable"},404);requirePermission(resolution.session,"connectors.manage");const body=await request.json().catch(()=>null) as Record<string,unknown>|null;if(!body||body.confirmation!==confirmation)return response(requestId,{ok:false,code:"explicit_confirmation_required"},400);const apiBase=String(process.env.TRACEKIT_API_BASE_URL||process.env.NEXT_PUBLIC_API_BASE_URL||"").replace(/\/$/,"");const secret=String(process.env.TK_SECRET_KEY||"").trim();if(!apiBase||!secret)return response(requestId,{ok:false,code:"operator_proxy_unavailable"},503);const upstream=await fetch(`${apiBase}/internal/commerce/ordering-verification`,{method:"POST",headers:{"content-type":"application/json","x-tk-secret":secret},body:JSON.stringify({confirmation})});const payload=await upstream.json().catch(()=>({})) as Record<string,unknown>;const allowed=new Set(["ordering_reservation_rejected","ordering_reserved_run_invalid","ordering_post_reservation_gate_rejected","ordering_queue_dispatch_failed"]);if(!upstream.ok)return response(requestId,{ok:false,status:upstream.status,code:allowed.has(String(payload.code))?String(payload.code):"ordering_verification_failed",run_id:typeof payload.run_id==="string"?payload.run_id:null},upstream.status>=500?502:409);return response(requestId,{ok:true,status:upstream.status,run_id:typeof payload.run_id==="string"?payload.run_id:null,dispatch_source:payload.dispatch_source==="operator_ordering_verification"?payload.dispatch_source:null,ordering_verification:payload.ordering_verification===true,max_pages:payload.max_pages===3?3:null,per_page:payload.per_page===100?100:null},202)}catch(error){if(error instanceof AuthorizationDeniedError)return response(requestId,{ok:false,code:"resource_unavailable"},404);return response(requestId,{ok:false,code:"ordering_verification_proxy_failed"},500)}}
