import type { OrderDeepLinkState,OrderSummary,OrderWorkspaceSnapshot } from "./types";const FOCUS=new Set(["ledger","attribution","timeline","tracking"]);
export function parseOrderDeepLink(value:URLSearchParams|string):OrderDeepLinkState{const p=typeof value==="string"?new URLSearchParams(value.startsWith("?")?value.slice(1):value):value;const f=p.get("focus");return{orderId:p.get("order_id"),focus:f&&FOCUS.has(f)?f as OrderDeepLinkState["focus"]:null,lineId:p.get("line"),attributionId:p.get("attribution"),eventId:p.get("event_id"),identifier:p.get("identifier"),customerId:p.get("customer_id"),offerId:p.get("offer_id"),drawerId:p.get("drawer"),search:p.get("search"),replay:p.get("replay")==="1"}}
export function normalizeOrderDeepLink(s:OrderDeepLinkState,list:OrderSummary[],snap?:OrderWorkspaceSnapshot|null):OrderDeepLinkState{const orderId=list.some(o=>o.id===s.orderId)?s.orderId:list[0]?.id||null;const lineId=snap?.ledger.some(l=>l.id===s.lineId)?s.lineId:null;const eventId=snap?.timeline.some(e=>e.id===s.eventId)?s.eventId:null;const identifier=snap?.identifiers.some(i=>i.value===s.identifier||i.id===s.identifier)?s.identifier:null;const customerId=s.customerId===snap?.relatedCustomer.id?s.customerId:null;const offerId=s.offerId===snap?.relatedOffer.id||list.some(o=>o.offerId===s.offerId)?s.offerId:null;const valid=new Set([...(snap?.ledger.map(l=>l.drawerId)||[]),...(snap?.timeline.map(e=>`event:${e.id}`)||[]),...(snap?.identifiers.map(i=>`identifier:${i.id}`)||[]),...(snap?.intelligence.map(i=>`intelligence:${i.id}`)||[]),"attribution","tracking"]);return{...s,orderId,lineId,eventId,identifier,customerId,offerId,drawerId:s.drawerId&&valid.has(s.drawerId)?s.drawerId:null,replay:s.replay&&Boolean(snap?.timeline.length)}}
export function orderDeepLinkHref(s:Partial<OrderDeepLinkState>,identityId:string){const p=new URLSearchParams();if(s.orderId)p.set("order_id",s.orderId);if(s.focus)p.set("focus",s.focus);if(s.lineId)p.set("line",s.lineId);if(s.attributionId)p.set("attribution",s.attributionId);if(s.eventId)p.set("event_id",s.eventId);if(s.identifier)p.set("identifier",s.identifier);if(s.customerId)p.set("customer_id",s.customerId);if(s.offerId)p.set("offer_id",s.offerId);if(s.drawerId)p.set("drawer",s.drawerId);if(s.search)p.set("search",s.search);if(s.replay)p.set("replay","1");p.set("dev_identity",identityId);return`/orders?${p}`}
