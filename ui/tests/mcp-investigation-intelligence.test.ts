import assert from"node:assert/strict";import test from"node:test";import{readFileSync}from"node:fs";
test("M7 investigation exposes evidence present missing boundaries and inspection targets",()=>{const r=readFileSync(new URL("../lib/mcp/journey-repository.ts",import.meta.url),"utf8");for(const x of ["TrackingInvestigation","observedConditions","presentEvidence","missingEvidence","boundaries","inspectionTargets","evidenceLimits"])assert.match(r,new RegExp(x));});
test("M7 tool is read-only and avoids unsupported root-cause claims",()=>{const t=readFileSync(new URL("../lib/mcp/tool-adapter.ts",import.meta.url),"utf8");assert.match(t,/tracekit\.investigate_tracking/);assert.match(t,/readOnlyHint:true/);const r=readFileSync(new URL("../lib/mcp/journey-repository.ts",import.meta.url),"utf8");assert.doesNotMatch(r,/rootCause|causedBy|blame|faultScore|repairAction/i);});
