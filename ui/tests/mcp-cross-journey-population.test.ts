import assert from"node:assert/strict";import test from"node:test";import{readFileSync}from"node:fs";
test("M6 population semantics distinguish scanned discovered analyzed skipped and failed",()=>{const r=readFileSync(new URL("../lib/mcp/journey-repository.ts",import.meta.url),"utf8");for(const x of ["customersRequested","customersScanned","journeysDiscovered","journeysAnalyzed","journeysSkippedByLimit","journeysFailed"])assert.match(r,new RegExp(x));assert.match(r,/for\(const journey of Array\.isArray\(detail\.journeys\)/);});
test("M6 tool exposes independent customer and total Journey bounds",()=>{const t=readFileSync(new URL("../lib/mcp/tool-adapter.ts",import.meta.url),"utf8");assert.match(t,/customer_limit/);assert.match(t,/journey_limit/);assert.match(t,/maximum:100/);assert.doesNotMatch(t,/analyzeJourneys\(\{limit:/);});
