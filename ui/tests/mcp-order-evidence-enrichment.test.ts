import assert from"node:assert/strict";import test from"node:test";import{readFileSync}from"node:fs";
test("M4 order workspace preserves journey evidence and commerce relationship separately",()=>{const s=readFileSync(new URL("../lib/mcp/order-repository.ts",import.meta.url),"utf8");assert.match(s,/journeys\[0\]/);assert.match(s,/\/journeys\//);assert.match(s,/Transaction ID/);assert.match(s,/Affiliate ID/);assert.match(s,/type:"Order"/);assert.match(s,/type:"Customer"/);assert.match(s,/Order linkage is preserved separately from attribution evidence/);});
test("M4 order enrichment does not invent unavailable financial detail",()=>{const s=readFileSync(new URL("../lib/mcp/order-repository.ts",import.meta.url),"utf8");assert.match(s,/mainProduct:"Not available from current Order read model"/);assert.match(s,/ledger:\[\]/);assert.match(s,/processor:"Not available"/);});
