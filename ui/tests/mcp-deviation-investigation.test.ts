import assert from"node:assert/strict";import test from"node:test";import{readFileSync}from"node:fs";
test("M7 deviation investigation revalidates observed deviation and bounds Journey investigations",()=>{const r=readFileSync(new URL("../lib/mcp/journey-repository.ts",import.meta.url),"utf8");for(const x of ["DeviationInvestigation","mcp_deviation_not_observed","supportingJourneyIds.slice(0,10)","journeysUnavailable","buildTrackingInvestigation"])assert.match(r,new RegExp(x.replace(/[()]/g,"\\$&")));});
test("M7 deviation tool is bounded read-only and requires evidence coordinates",()=>{const t=readFileSync(new URL("../lib/mcp/tool-adapter.ts",import.meta.url),"utf8");assert.match(t,/tracekit\.investigate_deviation/);for(const x of ["dimension","value","metric","customer_limit","journey_limit"])assert.match(t,new RegExp(x));assert.match(t,/readOnlyHint:true/);});
