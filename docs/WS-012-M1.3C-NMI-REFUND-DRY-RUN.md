# WS-012 / M1.3C — NMI refund evidence dry-run

This milestone adds no financial-event writer, route, RPC, migration, provider request, scheduler behavior, or production mutation. The Gateway Classic snapshot importer remains unchanged.

## Read-only command

From `api/`, with existing server-only Supabase read credentials in the process environment:

```sh
npm run dry-run:nmi-refunds -- --output=/private/tmp/nmi-refund-manifest.json
```

The command performs HTTP `GET` reads only. The optional manifest is created with mode `0600` and exclusive-create semantics. It contains provider transaction identifiers and evidence hashes, but no source XML or customer PII. Do not write the manifest into the repository.

## Production evidence result (2026-09-25)

- Retained refund records: 243
- Unique source IDs: 243
- Duplicate source IDs: 0
- `REFUND_SUCCEEDED`: 146
- `REFUND_FAILED`: 92
- `REFUND_PENDING`: 0
- `REFUND_AMBIGUOUS`: 5
- Parent resolved: 175
- Parent unresolved: 68
- Financial-fingerprint conflicts: 0
- Multiple-refund-action cases: 0
- Invalid amount/currency/timestamp cases: 0
- `BLOCKED_SCOPE`: 238
- `QUARANTINE`: 5
- Deterministic manifest hash: `1aead908a83566e9ac4fed7a63257c8eaca502639d231ec21deabb8cb9d9eb98`

The five ambiguous records have `condition=complete` and response code `100`, but use an uncertified `RETURN <code>` response-text family rather than the certified `APPROVED` response. They are intentionally not inferred to be successful.

## Scope gate

All ten named legacy NMI platforms are `SCOPE_MISSING`. Their credential rows exist and use key version 2, but:

- legacy credential metadata is empty;
- historical `platform_orders` have no `account_id`, `organization_id`, `connection_id`, or `provider_account_id`;
- no exact `commerce_provider_connections` mapping exists; and
- no exact `commerce_provider_accounts` mapping exists.

Names and labels are not ownership evidence. Financial writes remain prohibited until all four dimensions are deterministically resolved and joined.
