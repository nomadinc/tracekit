import assert from"node:assert/strict";import test from"node:test";
import{EVERFLOW_REPORT_HEADERS,normalizeEverflowRow}from"./connectors/everflow/historical-report.ts";
const row=Object.fromEntries(EVERFLOW_REPORT_HEADERS.map(h=>[h,""]));Object.assign(row,{date:"2026-04-01T12:00:00Z",click_date:"2026-04-01T11:30:00Z",conversion_id:"conversion-synthetic",transaction_id:"transaction-synthetic",email:"SYNTHETIC@EXAMPLE.INVALID",network_affiliate_name:"Synthetic Affiliate",sub1:"synthetic",sale_amount:"49",session_user_ip:"192.0.2.10",conversion_user_ip:"192.0.2.20"});
test("verified Everflow schema remains exact",()=>{assert.equal(EVERFLOW_REPORT_HEADERS.length,103);assert.equal(EVERFLOW_REPORT_HEADERS[0],"conversion_status");assert.equal(EVERFLOW_REPORT_HEADERS.at(-1),"is_view_through");});
test("row identity preserves event and Transaction distinction",()=>{const a=normalizeEverflowRow(row,2,"a".repeat(64)),b=normalizeEverflowRow({...row,event_name:"upsell"},3,"a".repeat(64));assert.equal(a.transactionId,b.transactionId);assert.notEqual(a.sourceIdentity,b.sourceIdentity);assert.equal(a.emailNormalized,"synthetic@example.invalid");assert.notEqual(a.sessionIpHash,"192.0.2.10");});
test("row identity is deterministic and source-bound",()=>{assert.equal(normalizeEverflowRow(row,2,"a".repeat(64)).sourceIdentity,normalizeEverflowRow(row,2,"a".repeat(64)).sourceIdentity);assert.notEqual(normalizeEverflowRow(row,2,"a".repeat(64)).sourceIdentity,normalizeEverflowRow(row,2,"b".repeat(64)).sourceIdentity);});
